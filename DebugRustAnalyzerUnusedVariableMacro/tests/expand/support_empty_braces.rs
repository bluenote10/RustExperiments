use debug_rust_analyzer_unused_variable_macro::{assemble_tree, Node};

pub fn main() {
    // Not really working yet...

    // let node: Node = assemble_tree!(
    //     Node::new("a") => {
    //     },
    // );

    // let node: Node = assemble_tree!(
    //     Node::new("a") => {
    //     }
    // );

    // let node: Node = assemble_tree!(
    //     Node::new("a") => {
    //         Node::new("b") => {
    //         },
    //     }
    // );

    // let node: Node = assemble_tree!(
    //     Node::new("a") => {
    //         Node::new("b") => {
    //         }
    //     }
    // );
}
